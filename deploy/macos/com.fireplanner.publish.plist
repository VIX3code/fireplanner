<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.fireplanner.publish</string>

  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>__SCRIPT__</string>
  </array>

  <!-- Weekdays at 16:35 local time: just after the US close, so the session's
       bar is complete. Running earlier ingests a half-formed bar; running much
       later gains nothing, since daily bars do not change again. -->
  <key>StartCalendarInterval</key>
  <array>
    <dict><key>Weekday</key><integer>1</integer><key>Hour</key><integer>16</integer><key>Minute</key><integer>35</integer></dict>
    <dict><key>Weekday</key><integer>2</integer><key>Hour</key><integer>16</integer><key>Minute</key><integer>35</integer></dict>
    <dict><key>Weekday</key><integer>3</integer><key>Hour</key><integer>16</integer><key>Minute</key><integer>35</integer></dict>
    <dict><key>Weekday</key><integer>4</integer><key>Hour</key><integer>16</integer><key>Minute</key><integer>35</integer></dict>
    <dict><key>Weekday</key><integer>5</integer><key>Hour</key><integer>16</integer><key>Minute</key><integer>35</integer></dict>
  </array>

  <!-- launchd fires a missed StartCalendarInterval once on wake, so a sleeping
       Mac catches up rather than silently skipping the day. -->
  <key>RunAtLoad</key>
  <false/>

  <key>StandardOutPath</key>
  <string>__LOGDIR__/fireplanner.launchd.out</string>
  <key>StandardErrorPath</key>
  <string>__LOGDIR__/fireplanner.launchd.err</string>

  <key>WorkingDirectory</key>
  <string>__WORKDIR__</string>

  <key>EnvironmentVariables</key>
  <dict>
    <!-- launchd starts with a minimal PATH; spell it out. /opt/homebrew/bin is
         where Homebrew installs on Apple Silicon, and is where node and the
         netlify CLI live if you installed them that way — leaving it off is a
         "command not found" that only ever shows up in the log. -->
    <key>PATH</key>
    <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
    <key>IB_PORT</key>
    <string>__IBPORT__</string>
  </dict>

  <!-- One run at a time. A slow gateway must not stack up jobs. -->
  <key>AbandonProcessGroup</key>
  <false/>
</dict>
</plist>
