__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.cache/
.venv/
venv/
.env
# Generated pages — they render account-derived figures, so they are
# artifacts to publish, never files to track.
dashboard.html
signal_desk.html
*.parquet
.DS_Store

# Live account state (balances, positions) — never commit
data/snapshots/account.json

# deployment secrets and machine-local state
deploy/.env
deploy/macos/fireplanner.env
deploy/macos/fireplanner-venv/
deploy/macos/fireplanner_site/
deploy/macos/*.log
deploy/macos/fireplanner.launchd.*
site/
